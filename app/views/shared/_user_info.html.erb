<%= link_to gravatar_for(current_user, size: 50), user_profile_url(current_user.slug) %>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", user_profile_url(current_user.slug) %></span>
<span><%= pluralize(current_user.microposts.count, "micropost")%></span>
