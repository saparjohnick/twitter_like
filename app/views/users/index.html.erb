<% provide :title, "All users" %>

<h1>All users (<%= @count %>)</h1>

<%= paginate @users %>

  <ul class="users">
    <%= render @users %> 
  </ul>

<%= paginate @users %>

<%# Rails делает вывод о том, что @users — это список объектов User;
 более того, при вызове с коллекцией пользователей, Rails автоматически 
 перебирает ее и отображает каждого с помощью партиал _user.html.erb %>
