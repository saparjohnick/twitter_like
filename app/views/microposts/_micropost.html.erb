<li id="micropost_<%= micropost.id%>">
  <%= link_to gravatar_for(micropost.user, size: 50), user_profile_url(micropost.user.id) %>
  <span class="user"><%= link_to micropost.user.name, user_profile_url(micropost.user.id) %></span>
  <span class="content"><%=  micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  <span>
</li>
